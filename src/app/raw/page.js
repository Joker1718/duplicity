import SaveRequiredPage from "@/components/save-required-page";

export default function RawEditorPage() {
  return (
    <SaveRequiredPage
      title="Raw Editor"
      description="Raw object tree, breadcrumb navigation, and primitive field editors will be ported here."
    />
  );
}
